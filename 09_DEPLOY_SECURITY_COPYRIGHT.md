# 部署、可观测性、安全、隐私与版权

## 1. 部署拓扑

### 1.1 开发

Docker Compose 启动 Web、API、两个 Worker、Postgres、OpenSearch、Redis、MinIO 和监控最小组件。只加载合成或明确可用于开发的数据。

### 1.2 生产

- Web/API/Worker 运行在私有网络的托管容器或 Kubernetes。
- 数据库、搜索、对象存储使用托管服务优先；启用加密和版本控制。
- API 经 WAF/反向代理；Worker 不接受公网入站。
- OCR/视觉解析运行在隔离池，限制 CPU、内存、磁盘、网络和时长。
- 开发、预发布、生产账号与密钥完全分离。

## 2. 配置与密钥

- 密钥存 Secret Manager，不入仓库、镜像或日志。
- 短效工作负载身份优先于静态 access key。
- 数据商、模型、对象存储凭据按组件最小授权。
- 定期轮换；泄露演练包含撤销、重发、审计和影响分析。
- 启动时校验配置；生产禁止 debug、默认密码和公共 bucket。

## 3. 身份与授权

- OIDC/SAML（组织部署）或安全会话认证。
- RBAC + 资源属性：tenant、source entitlement、document visibility。
- 权限在 API 和异步 Worker 再检查，不能只靠前端。
- 临时签名 URL 生命周期短，生成前检查权限；对象键不可猜。
- 管理操作、导出、来源访问、审校和删除均记审计日志。

## 4. 威胁模型重点

| 威胁 | 控制 |
|---|---|
| 恶意 PDF/图片利用解析器 | 沙箱、无网络、资源限制、补丁、文件扫描 |
| Prompt injection | 来源作数据处理、固定工具编排、引用验证 |
| 跨租户检索泄露 | 召回前强制过滤、缓存隔离、权限测试 |
| 对象存储直链泄露 | 私有 bucket、短签名、不可预测 ID、审计 |
| 任意 DSL 执行 | JSON AST 白名单、无 eval、资源上限 |
| 回测 DoS | 配额、行数估算、队列优先级、取消和超时 |
| 供应链攻击 | 锁依赖、SBOM、签名镜像、漏洞门禁 |
| 日志泄露原文/密钥 | 结构化脱敏、字段白名单、访问控制 |

## 5. 版权与内容治理

### 5.1 导入前提

每个来源必须有 `EntitlementRecord`：

- 权利人/来源；
- 授权基础与证明位置；
- 允许用户/地区/用途；
- 是否允许 OCR、嵌入、模型处理、缓存、导出；
- 到期和撤销方式；
- 是否允许第三方云模型接触内容。

不清楚时状态为 `quarantined`，不进入索引或模型。

### 5.2 最小必要使用

- RAG 默认返回短证据片段和总结，不提供连续大段原文。
- 相邻上下文展开有窗口、速率和权限限制。
- Wiki 不应通过连续摘录替代原书。
- 导出包含来源、版次、使用限制和水印。
- 不用受限全文训练自有模型，除非授权明确覆盖。

### 5.3 模型与供应商

发送给外部 OCR/embedding/LLM 前检查授权：

- 是否用于供应商训练；
- 数据保留时长和地区；
- 子处理商；
- 删除承诺；
- 企业隐私模式。

不满足时使用本地模型或禁止该流程。调用日志只存最小元数据，不复制受限正文。

### 5.4 撤销与删除

来源撤销流程：

1. 立即改为不可检索、不可查看；
2. 失效缓存和签名 URL；
3. 删除全文/向量/索引派生物；
4. 识别依赖 Wiki/规则，标记引用不可用或重新审校；
5. 按政策删除对象和备份到期；
6. 生成可审计完成报告。

保留最小审计记录时，不应保留可重建原文的字段。

## 6. 隐私

截图可能包含账户名、自选列表、通知或设备信息：

- 上传后去 EXIF；
- 提供裁剪/自动遮盖工具；
- 默认不用于训练；
- 明确保留期限和删除入口；
- 管理员不能无审计浏览用户上传。

不要将文件名、查询和截图内容无过滤发送到遥测平台。

## 7. 可观测性

### 7.1 指标

- API latency/error/rate；
- 队列深度、等待和任务失败；
- OCR 页吞吐、低置信度比例；
- 检索各阶段耗时与空结果率；
- 模型 token/成本/拒绝率；
- 扫描 bars/sec、回测耗时、资源；
- 权限拒绝、导出和删除 SLA。

### 7.2 日志与链路

结构化日志包含 request/job/tenant/entity ID，不含正文、凭据和完整查询。OpenTelemetry 贯穿 API→队列→Worker→存储。高风险审计日志写入不可篡改或独立存储。

### 7.3 告警

- 跨租户权限异常或受限对象公共访问：立即 Sev-1；
- 导入/回测失败率、队列积压、存储容量、备份失败；
- RAG 引用验证失败骤升；
- 数据源延迟或质量异常。

## 8. 备份、恢复和灾难演练

- Postgres PITR；对象存储版本控制；配置和 IaC 入库。
- OpenSearch 与向量索引视为可重建，但保存索引 manifest。
- 定义 RPO/RTO：MVP 建议 RPO 24h、RTO 8h；生产目标按业务调整。
- 每季度恢复演练：新环境恢复数据库、对象、索引，再跑黄金查询和回测。

## 9. 发布与回滚

- 数据库迁移采用 expand/contract，先兼容后切换。
- 模型、索引和规则用版本指针切换，可回滚。
- 灰度发布监控错误、引用质量和任务队列。
- 回滚不能让新 revision 的数据不可读；破坏性迁移延后。

## 10. DoD

- 威胁模型、数据流图、授权矩阵和供应商清单审核完成。
- 跨租户、越权、签名 URL、上传解析、Prompt injection 和 DSL 沙箱测试通过。
- SBOM、漏洞报告、备份恢复和来源删除演练有记录。
- 生产日志抽检不含受限原文、密钥或隐私截图。
- 版权/免责声明文案经适当法务或权利负责人确认。

