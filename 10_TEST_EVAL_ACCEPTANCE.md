# 测试、评测与验收标准

## 1. 质量策略

测试分为五层：

1. 单元：纯函数、Schema、时间和数学边界。
2. 契约：API、事件、数据库迁移、DSL。
3. 集成：对象存储、索引、任务幂等、权限。
4. 端到端：三条完整用户闭环。
5. 离线评测：OCR、检索/RAG、视觉、规则和回测可信度。

任何模型指标都按数据集版本、子集、置信区间报告；不可只给一个总体平均数。

## 2. 黄金数据集

### 2.1 OCR/页码集

- 至少 100 页，覆盖原生、扫描、双栏、图注、脚注、旋转和不同页码段。
- 每页有人审文本、阅读顺序、印刷页码和关键 bbox。
- 若受版权限制，仓库只保存加密/访问受控 manifest 和合成替代样本。

### 2.2 RAG 集

至少 150 个问题：

- 术语定义 30；
- 形态比较 30；
- 限定条件/例外 30；
- 跨来源差异 20；
- 需要多证据组合 20；
- 明确不可回答 20。

每题记录可接受 claims、必要/可选证据、禁止误解和答案可回答性。

### 2.3 规则集

每个 MVP 规则至少 20 个小型序列：正例、边界、逐条件反例、相似形态、缺失/零值、时间确认。

### 2.4 视觉集

至少 1,000 张合成/扰动标准图和 200 张有授权真实截图；按模板与底层序列分组切分。

### 2.5 回测集

- 手算小市场；
- 含停牌、拆股、分红、退市和成分变化的合成市场；
- 特意注入未来泄漏的“坏实现”回归样例。

## 3. MVP 指标门禁

以下是建议起始门槛；M0 用真实样本校准后冻结，降低门槛需 ADR 和风险接受。

| 域 | 指标 | P0 门槛 |
|---|---|---:|
| 页面 | 页面渲染覆盖 | 100% 或逐页明确失败 |
| 页码 | 黄金页印刷页码准确率 | ≥ 99% |
| OCR | 关键中文正文 CER | ≤ 3% |
| 版面 | 关键证据 bbox 命中率 | ≥ 97% |
| 检索 | Recall@10（必要证据） | ≥ 90% |
| 重排 | nDCG@10 | ≥ 0.85 |
| RAG | claim 引用覆盖率 | ≥ 98% |
| RAG | citation correctness | ≥ 95% |
| RAG | 不可回答正确拒答 | ≥ 90% |
| 规则 | 黄金向量一致率 | 100% |
| 一致性 | 扫描/回测/解释同输入一致 | 100% |
| 时间 | 未来函数强制测试 | 100% 通过 |
| 回测 | 手算交易/权益一致 | 100%（容许数值误差） |
| 视觉 | 合成标准图蜡烛 F1 | ≥ 0.97 |
| 视觉 | 标准图规则结果一致率 | ≥ 95% |
| 视觉 | 不支持输入正确拒绝率 | ≥ 90% |
| 安全 | 跨租户与越权关键测试 | 100% |
| 可靠性 | 固定 manifest 重跑一致 | 100% |

真实截图指标按主题/平台分层报告；MVP 可把“真实截图可解析率”作为观测指标，但不能把低置信度结果包装为准确结论。

## 4. 性能与容量假设

M0 冻结目标负载。建议 MVP 基准：

- 10 本书、5,000 页、100,000 evidence chunks；
- 5,000 个标的、10 年日线；
- 20 并发交互用户、5 个长任务并行。

建议 SLO：

- Wiki/证据元数据 p95 < 500ms；
- 混合检索（不含生成）p95 < 2s；
- RAG 首 token p95 < 5s；
- 单规则扫描 5,000 标的×10 年在约 10 分钟内（硬件基准固定）；
- 普通单策略回测在约 5 分钟内；
- 任务状态可用性 99.5%（MVP）。

性能测试报告必须注明硬件、数据规模、冷热缓存和版本。

## 5. 端到端验收场景

### E2E-1：证据与问答

上传样例书 → 处理 → 修正页码 → 创建 Wiki 条目 → 审批 → 提问 → 获取带引用回答 → 打开正确页面高亮 → 撤销来源后不可再访问。

### E2E-2：规则、扫描与回测

从证据创建规则 → 编译 → 黄金测试 → 导入行情快照 → 扫描候选 → 查看逐项解释 → 以同一 revision 回测 → 重跑结果一致。

### E2E-3：截图

上传标准截图 → 检测 pane → 用户确认颜色/周期 → 重建蜡烛 → 规则候选 → 展示叠加与不确定性 → 对低质量版本正确降级。

### E2E-4：权限

两个租户拥有不同来源；检索、缓存、任务、签名 URL、导出和日志均无法跨租户访问。

## 6. 发布门禁

P0 必须全部通过：

- 没有开放 Sev-1/Sev-2 安全问题；
- 没有已知未来函数或规则语义分叉；
- 引用和版权门禁通过；
- 数据迁移、备份恢复、来源删除演练通过；
- 关键 E2E 在 staging 通过；
- 已知限制和用户文案完成；
- 产品、技术、QA、安全/版权责任人签字。

P1 可带风险发布，但必须有 owner、期限和缓解；P0 不可口头豁免。

## 7. 评测可重复性

- 数据集版本、样本 ID、标注指南和审校者记录。
- 模型/提示/索引/embedding/规则/代码全部固定。
- 评测脚本输出机器可读 JSON 和人类报告。
- CI 运行小型 smoke eval；每晚/发布前运行完整 eval。
- 指标退化超过阈值自动阻止晋级。

## 8. 缺陷分级

- Sev-1：跨租户/受限内容泄露、结果伪造、广泛数据损坏。
- Sev-2：未来函数、错误引用支撑关键结论、规则跨入口不一致、无法恢复。
- Sev-3：局部功能错误、有明确绕行。
- Sev-4：显示、文案和低影响问题。

## 9. 项目最终验收清单

- [ ] 所有专题 DoD 完成并有证据链接。
- [ ] P0 指标与四条 E2E 场景通过。
- [ ] 规则、数据、模型和来源版本可追溯。
- [ ] 文档、API、运行手册和部署清单与实现一致。
- [ ] 版权授权、免责声明、用户数据保留和删除机制已确认。
- [ ] 新团队可仅凭仓库文档在一天内启动本地栈并跑通样例。

