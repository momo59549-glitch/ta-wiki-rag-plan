# 产品目标、范围与非目标

> v2 产品更新：系统已定义为“多 Agent 股票研究团队”。书籍/Wiki/RAG 是证据与规则起点，研究数据库和可验证经验闭环是核心。

## 1. 问题定义

技术分析书籍常同时包含叙述性定义、限定条件、例外、图例和交易建议。简单 OCR + 聊天会导致四类问题：

- 找得到文字但回不到准确页面；
- 模型把相似概念混在一起；
- 书面描述无法直接变成确定性计算；
- 图表识别、行情扫描与回测各自实现一套“同名规则”。

本项目把“知识证据层”和“量化执行层”分开，再通过版本化的语义映射连接。任何可执行规则都应说明它是对书面定义的哪一种计算近似，而不是冒充唯一正确解释。

## 2. 产品原则

1. 原文证据先于总结；总结先于规则；规则先于回测。
2. 事实、作者观点、编辑注释、实现假设、模型推断必须分层标记。
3. 相对形态判断与绝对价格判断分开；截图不能可靠恢复的信息不猜测。
4. “没有足够证据”是合法输出。
5. 所有结果展示版本、时间范围、数据源、规则参数和置信度。
6. 教育与研究定位贯穿产品文案、权限和导出能力。

## 3. 用户故事

### 3.1 学习者

- 我搜索“锤子线和上吊线区别”，看到结构化对比、原文证据和适用上下文。
- 我展开引用时，系统定位到具体版本、印刷页码和页面高亮框。
- 当不同书籍给出不同要求时，我看到各自来源，而不是被合并成伪共识。

### 3.2 研究员

- 我将 Wiki 中的形态实例化为规则版本，查看默认阈值和实现假设。
- 我用固定数据快照扫描候选，并检查每个条件的实际值、阈值和通过状态。
- 我用同一规则版本回测，得到可复现清单、交易明细、成本和偏差警告。

### 3.3 编辑/审校员

- 我上传授权资料，查看 OCR 低置信度页和页码映射异常。
- 我从证据片段创建或更新 Wiki 条目，对 AI 草稿逐项批准。
- 我能将错误引用、重复条目和冲突定义标记并回滚。

### 3.4 管理员

- 我配置内容授权范围、用户角色、导出限制和保留期限。
- 我查看谁在何时访问或导出了哪些受限证据。
- 我撤销一个来源后，能重建索引并证明其内容已不可检索。

## 4. 功能范围

### 4.1 内容与知识

- PDF/图片批量导入、OCR、版面区块、页码映射、目录识别。
- 证据片段、句级坐标、表格/图注标记、人工纠错。
- Wiki 条目、别名、关系、规则映射、冲突与审校状态。
- 混合检索、过滤、重排、引用回答、相邻上下文展开。

### 4.2 市场与规则

- OHLCV 导入/拉取、标准化、质量检查、日历、公司行动。
- 形态 DSL、上下文条件、阈值参数、候选扫描、逐项解释。
- K 线截图上传、质量判断、坐标/蜡烛解析、候选验证。
- 历史回测、成本、基准、交易明细、稳定性和未来函数检查。

### 4.3 平台

- Web UI、REST API、异步任务、RBAC、审计、监控和备份。
- 数据、规则、模型、索引与回测产物的版本管理。

## 5. 明确非目标

- 任意格式电子书的完美自动解析。
- 复制并向无授权用户提供完整原文。
- 从单张无坐标截图可靠识别证券、交易所、周期和精确 OHLC。
- 用回测代替统计因果证明。
- 自动调参直到历史收益最好。
- 将大模型输出直接作为可执行交易逻辑。
- MVP 的实时流处理、实盘下单、账户管理和监管报送。

## 6. 权限角色

| 角色 | 查看 Wiki | 查看受限证据 | 编辑/审校 | 运行扫描/回测 | 管理授权 |
|---|---:|---:|---:|---:|---:|
| Viewer | 是 | 按授权 | 否 | 否 | 否 |
| Researcher | 是 | 按授权 | 建议草稿 | 是 | 否 |
| Editor | 是 | 是 | 编辑 | 是 | 否 |
| Reviewer | 是 | 是 | 审批/退回 | 是 | 否 |
| Admin | 是 | 是 | 是 | 是 | 是 |

服务账户使用最小权限，不继承人的管理权限。

## 7. 核心产品指标

- 首次成功导入时间、需要人工干预的页面比例。
- Wiki 条目证据覆盖率、审校吞吐、冲突率。
- RAG 引用准确率、拒答准确率、用户点击证据率。
- 规则扫描与回测结果一致性、候选人工确认率。
- 截图可解析率与校准后 precision/recall；低质量拒绝率。
- 回测复现成功率、因果测试通过率。
- 受限内容异常导出事件数、来源撤销完成时间。

指标不得以“用户运行更多回测”或“得到更高历史收益”为北极星；推荐“可验证研究闭环完成率”。

## 8. 关键产品决策

- Wiki 条目可以陈述定性知识；DSL 只能表达已定义、可计算且有边界的近似。
- RAG 回答必须先生成 claims，再为每个 claim 绑定证据；无法绑定的 claim 删除或明确标注为推断。
- 截图入口默认要求用户确认市场/周期/时间范围；若不可确认，仅输出相对几何形态。
- 回测默认使用下一可交易 bar 执行；同一 bar 的收盘信号不得按该收盘价无摩擦成交。
- 书中交易建议以“作者观点”呈现，不转化为产品推荐文案。
